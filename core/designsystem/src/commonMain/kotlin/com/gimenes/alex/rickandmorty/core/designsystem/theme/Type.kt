package com.gimenes.alex.rickandmorty.core.designsystem.theme

import androidx.compose.material3.Typography

/**
 * Accessibility-first: we intentionally keep Material3's default type scale (system font,
 * default sizes/line-heights) rather than a custom brand type ramp, so user font-scale and
 * platform accessibility settings continue to work as expected for body/UI text.
 */
val RickAndMortyTypography = Typography()
